
# Generator of fptk local

This script creates `/fptk_local` folder with inner imports updated accordingly.
Generated files are placed to `/_etc/local_versions/`

This is needed for using `fptk_local` in other projects to have stable fptk inside them.
This is until fptk will reach stable release.

