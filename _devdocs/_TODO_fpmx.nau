
// TODO: doc_gen ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾\ {{{1

	Prettify:
	+ docgen should not find values in ;; (setv ...) JFK
	+ Remove requirement for `;;` comments to have double-semicolon
	+ Enhance recognition of if `#_ "comment"` belongs to prev `(import ...)` or next `(defn ...)`
	* "FPMX original" vs "Macro from: FPMX" 
	* Somehow remove manual «05 Title: subtitle» sorting requirement? Or at least add warning when both «05 Title» and «06 Title» exist?

	New:
	+ (help (get_macro def::))

// ____________________________________________________________________________/ }}}1
// TODO: fpmx ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾\ {{{1

	## Macros
	+ getattrm not needed?
	? (=> Point .x.val (.x.val)) what will happen?

	## Lens
	- GetAttr "kind"
  	- &+ inner implementation: (get x) -> (& (last expr) (. lens (get x)))
	- hide lens inside macro JFK
	- lens: (- 7) -> remove

	## Lists
		+ funcy.interpose (str_join is for strings), inteleave (x1y2z3), interpose (x-y-z)
	    + (flatten xs :level 3)
		+ insert
		+ enlengthen_list
		+ [1 2 3] [a b c] -> [1 2 3 a] [b c] ; to_left, to_right
		+ count_occurrences -> count_by, count_reps, count_elem

	## Fix
		+ lprint: when on non-iterable, print normally
		- rest returns iterator, drop returns list, wtf
		- lbisect_by: option to include/exclude first found; negation in f in unclear
		- strip/lstrip/rstrip should also pass kwargs and such
		- all the iterator vs sequence and such
        - looks like cut_ is bad? no?

	## New
		+ both-ends-included ranges: range_ ? lrange_ ? inf_range_ ?

	## Dicts
		+ dkeys, dvalues, ditems
		+ add/substract

	## Typing
		+ (defn validationOFF [f] 
			  (defn wrapper [#* args #** kwargs]
					(return (f #* args #** kwargs)))
			  (return wrapper))

	@@ Docs
	- key approach: .attr

	@@ Tests
	+ tests for strict/non-strict monads
	+ isinstQ, approx_eq, round_to


// ____________________________________________________________________________/ }}}1

	✓ when incorrect option for load_fpmx -> throw error

	fpmx:
		- docs: main
		- docs: monads
		- publish (pypi, changelog/license, reddit)
		- MIT: do whatever, just don't sue me
	monads:
		- finish resultM
		- maybeWriter (tell)
		- .bind = bindM?
	docs main:
		- fpmx-exclusive macro, not Prelude
		- fpmx first header ugly
		- reorganize all in one Feature Highlight,
		  all topics should be visible
		- status: ready, but unpolished
		+ def:: has decorator

	* monads: make optional [] in (WNothing []) and (WJust 3 [])
	* upd docs: monads
	* upd docs: scripts (new script: nsmg)

	monads:
	- .bind should returns NEW monad objects (so .bind is just syntax-chaining, not state change)
	- possible .log_ mut/immut issues

